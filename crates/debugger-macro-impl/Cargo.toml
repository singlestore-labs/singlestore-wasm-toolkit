[package]
name = "debugger-macro-impl"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = [
    "full",
    "printing",
    "visit",
    "extra-traits",
    "parsing",
] }
quote = "1.0"
proc-macro2 = "1.0"
