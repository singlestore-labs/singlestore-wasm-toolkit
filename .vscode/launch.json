{
  "version": "0.2.0",
  "configurations": [
    {
      "preLaunchTask": "build: all",
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/target/debug/debugger",
      "args": ["${relativeFileDirname}"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
